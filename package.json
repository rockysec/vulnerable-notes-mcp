{
  "name": "vulnerable-notes-mcp",
  "private": true,
  "type": "module",
  "description": "MCP server deliberadamente vulnerable (tool poisoning, command injection, path traversal) con su versión corregida, para el post 'Auditando un servidor MCP' de rockysec.com",
  "scripts": {
    "start": "tsx src/vulnerable.ts",
    "start:fixed": "tsx src/fixed.ts",
    "start:http": "tsx src/vulnerable-http.ts",
    "start:http:fixed": "tsx src/fixed-http.ts",
    "agent-demo": "node agent-demo.mjs"
  },
  "dependencies": {
    "@ai-sdk/mcp": "^2.0.16",
    "@ai-sdk/openai": "^4.0.27",
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "ai": "^7.0.34",
    "tsx": "^4.23.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^2.0.0"
  }
}
